import { Login, Register } from '../pages'

const publicRoutes = {
  home: {
    path: '/login',
    component: Login
  },
  register: {
    path: '/register',
    component: Register
  }

}

export default publicRoutes
